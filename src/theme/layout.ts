/** Horizontal padding from screen edge (plus safe-area insets in screens). */
export const SCREEN_GUTTER = 16;

/** Breakpoint: stack dense finance/weather layouts below this width. */
export const COMPACT_WIDTH = 400;
